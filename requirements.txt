#
# Requisitos mínimos para este proyecto (face_tracking.py y test_detect_clothes.py)
# Solo se listan las dependencias que realmente se importan en el código.
# Versiones fijadas a las que tienes ahora en tu entorno.
#

numpy==2.4.2
opencv-python==4.13.0.92
pillow==12.1.1

torch==2.10.0
torchvision==0.25.0

ultralytics==8.4.19

insightface==0.7.3
onnx==1.20.1
onnxruntime==1.24.2

# tkinter viene con Python estándar.
# En Debian/Ubuntu, si falta:
#   sudo apt install python3-tk

